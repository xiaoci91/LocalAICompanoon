# Local AI Companion - 开发风险点清单

## 一、安全风险（最高优先级）

### 1.1 大模型幻觉导致存档破坏
**风险等级**：极高
**风险描述**：大模型可能产生幻觉，输出恶意或错误的指令，导致AI同伴胡乱破坏方块、炸毁玩家基地。
**规避措施**：
- 四层隔离架构，LLM输出必须经过安全校验层
- 所有高风险权限默认关闭
- 方块黑名单机制，默认保护60+种重要方块
- 安全区域系统，可自定义保护区
- 最大解析重试次数，解析失败直接丢弃

### 1.2 权限绕过
**风险等级**：高
**风险描述**：大模型可能通过特殊格式的输出来绕过安全校验。
**规避措施**：
- 严格的JSON格式校验，格式不对直接拒绝
- 多层校验：模式校验 → 服务器校验 → 权限校验 → 黑名单校验 → 安全区校验
- 白名单机制，只允许预定义的意图类型
- 未知意图直接当聊天处理，不执行任何操作

### 1.3 注入攻击
**风险等级**：中
**风险描述**：玩家可能通过聊天内容注入恶意指令，让AI做不该做的事。
**规避措施**：
- 系统提示词明确行为边界
- 安全校验层不依赖LLM判断，完全由代码硬逻辑控制
- 所有操作都要经过权限检查

## 二、性能风险

### 2.1 主线程阻塞
**风险等级**：极高
**风险描述**：LLM网络请求如果在主线程执行，会导致游戏卡顿甚至冻结。
**规避措施**：
- 所有网络请求全部在后台异步线程执行
- 主线程只负责渲染和实体更新
- 回调通过服务器调度器回到主线程执行游戏操作
- 请求限流，同一时间最多1个请求

### 2.2 内存泄漏
**风险等级**：高
**风险描述**：对话历史、记忆数据不断增长，导致内存占用过高。
**规避措施**：
- 短期记忆有最大数量限制（默认20条）
- 长期记忆持久化到磁盘，不全部加载到内存
- 任务历史只保留最近50条
- 定期清理过期数据

### 2.3 任务死循环
**风险等级**：高
**风险描述**：寻路或任务执行出现死循环，导致AI卡住不动。
**规避措施**：
- 每个任务有独立超时时间（默认120秒）
- 超时自动终止任务，NPC回到玩家身边
- 最大重试次数限制
- 任务状态机，状态转换有明确边界

## 三、兼容性风险

### 3.1 第三方模组方块
**风险等级**：中
**风险描述**：其他模组添加的方块，AI可能不认识或无法正确处理。
**规避措施**：
- 提供方块-别名映射JSON，玩家手动填写
- 不做自动识别，避免误判
- 未知方块默认不允许破坏

### 3.2 Baritone版本不兼容
**风险等级**：中
**风险描述**：Baritone API变更导致寻路功能失效。
**规避措施**：
- 自研抽象层隔离Baritone底层
- 反射调用，避免编译期依赖
- 底层库升级时只需要修改适配层

### 3.3 多模组冲突
**风险等级**：中
**风险描述**：和其他修改实体行为的模组产生冲突。
**规避措施**：
- 尽量使用标准API
- 不修改原版实体的核心逻辑
- Mixin尽量少，只在必要时使用

## 四、网络风险

### 4.1 LLM服务断开
**风险等级**：中
**风险描述**：Ollama服务崩溃或端口被占用，导致AI不回应。
**规避措施**：
- 自动重试机制
- 服务断开时游戏不会崩溃，只显示提示
- 已接收的任务可以继续执行
- 自动探测服务状态

### 4.2 显存OOM
**风险等级**：中
**风险描述**：模型太大导致显存溢出，Ollama服务崩溃。
**规避措施**：
- 识别OOM错误，向玩家提示
- 建议切换更小量化的模型
- 不会导致游戏崩溃

### 4.3 请求超时
**风险等级**：低
**风险描述**：模型生成太慢，请求超时。
**规避措施**：
- 可配置的超时时间
- 超时自动重试（最多3次）
- 重试失败给玩家提示

## 五、数据风险

### 5.1 存档损坏
**风险等级**：高
**风险描述**：模组数据写入异常导致存档损坏。
**规避措施**：
- 所有数据保存在独立子目录，不修改原版存档文件
- JSON格式存储，损坏了也可以手动修复
- 自动备份机制
- 写入失败时不影响游戏正常运行

### 5.2 记忆丢失
**风险等级**：中
**风险描述**：游戏崩溃导致记忆数据没有保存。
**规避措施**：
- 定期自动保存（默认5分钟）
- 重要操作即时保存
- 退出游戏时强制保存
- 数据写入原子操作

## 六、用户体验风险

### 6.1 上手门槛高
**风险等级**：高
**风险描述**：普通玩家不会配置Ollama和模型参数。
**规避措施**：
- 6档硬件预设，一键切换
- 自动探测本地服务
- 首页醒目提示安装方法
- 详细的使用说明书
- 默认配置就能跑

### 6.2 AI回答质量差
**风险等级**：中
**风险描述**：小模型回答质量不理想，玩家体验不好。
**规避措施**：
- 精心设计的系统提示词
- 长期记忆补充上下文
- 输出格式严格约束
- 允许玩家自定义系统提示词

### 6.3 响应速度慢
**风险等级**：中
**风险描述**：CPU或低端显卡运行大模型太慢。
**规避措施**：
- 请求限流，避免排队
- 异步处理，不阻塞游戏
- 硬件预设推荐合适的模型
- 显示"思考中"状态

## 七、开发风险

### 7.1 架构过于复杂
**风险等级**：中
**风险描述**：四层架构增加了开发复杂度和调试难度。
**规避措施**：
- 每层职责清晰，接口明确
- 单元测试覆盖核心逻辑
- 详细的日志输出
- 模块化设计，可单独调试

### 7.2 Baritone依赖过重
**风险等级**：中
**风险描述**：很多功能依赖Baritone，如果Baritone出问题就全完了。
**规避措施**：
- 抽象层隔离，可以替换底层寻路库
- 简单任务可以不依赖Baritone
- 预留自研寻路的扩展空间

### 7.3 功能范围蔓延
**风险等级**：中
**风险描述**：不断加功能，导致项目越来越大，bug越来越多。
**规避措施**：
- 明确的功能边界
- 不做大型建筑生成
- 不支持公网服务器
- 核心功能优先，次要功能后置

## 八、法律风险

### 8.1 模型版权
**风险等级**：低
**风险描述**：使用的模型可能有版权问题。
**规避措施**：
- 不打包任何模型权重
- 模型由用户自行下载
- 只提供接口，不提供模型

### 8.2 数据隐私
**风险等级**：低
**风险描述**：聊天数据和记忆可能包含用户隐私。
**规避措施**：
- 所有数据全部本地存储
- 不上传任何数据到云端
- 用户可以随时删除记忆
- 明确的隐私说明
